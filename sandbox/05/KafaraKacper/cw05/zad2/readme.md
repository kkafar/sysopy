1. Kompilacja poleceniam `make` 
2. Program respektuje skladnie zgodnie z specyfikacja zadania (mozna wykonac program bez zadnych paramterow, zostanie wtedy wypisany komunikat z opisem przyjmowanych parametrow.)
3. Program wymaga zainstalowanego narzedzia `mailx` (pakiet `mailutils`)
4. `make clean` usuwa wszystkie pliki z rozszerzeniem .o, `make clean_all` usuwa dodatkowo wszystkie pliki wykonywalne