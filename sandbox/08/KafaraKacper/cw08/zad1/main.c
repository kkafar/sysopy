#include <stdlib.h>
#include <pthread.h>
#include <stdio.h>
#include <math.h>
#include "util.h"


int main(int argc, char * argv[])
{
    if (argc != 5) 
        err("Invalid number of arguments.", __FILE__, __func__, __LINE__);
        
    exit(EXIT_SUCCESS);
}